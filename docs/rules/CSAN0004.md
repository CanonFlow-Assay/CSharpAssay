# CSAN0004 — Nullable public core contract

Status: Admitted
Category: Nullability
Certainty: deterministic candidate
Disposition: block only after admission and only within qualified scope

## Mechanism

Public symbols are inspected recursively for annotated nullable values, including nested generic arguments.

## False-positive boundary

The runner changes the disposition outside configured core, but still records the evidence.

## Evidence

- Compliant specimen: `specimens/Compat.Good/CSAN0004/`
- Violation specimen: `specimens/Compat.Bad/CSAN0004/`
- Suppression specimen: `specimens/Compat.Bad/CSAN0004/Suppression.cs`
- Fault specimen: `specimens/Faults/CSAN0004/Malformed.cs`

## Suppression

A compiler pragma or `SuppressMessage` is not authorization. Suppression must
match a reviewed policy grant with a finding fingerprint, owner, reason, and
expiry; otherwise CSAP0001 remains evidence.

## Fix safety

No automatic fix. Move interop nullability to the shell and expose an explicit domain type.
